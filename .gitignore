# Generated by the monitor; intentionally not source-controlled (rebuilt on
# each archive sweep). Was showing up as untracked in `git status` and adding
# noise — see https://github.com/HenryMaxPaterson/vf-quotes/issues if you ever
# move this elsewhere.
.archive-manifest.json

# Local preview output from test_local.sh — not for the public site.
preview/
